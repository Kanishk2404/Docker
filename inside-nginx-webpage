mkdir nginx-web
cd nginx-web
nano index.html
touch Dockerfile

FROM nginx:latest
COPY index.html /usr/share/nginx/html/index.html or COPY . /usr/share/nginx/html as . copies who directory in the path we give.

docker build -t my first .

docker run -d -p 80:80 --name simplenginx my-first
